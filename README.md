# CDG-315-Final

# When loading into the game scene, use the WASD buttons to control your vehicle's movement around the track.

# The objective is to complete three laps around the track faster than the other player(s).

# To view the singleton setup, look in the GameManager script.

# To view the delegate set up, look in the LapTracker, FinishLine, and HUD scripts.

# To view audio implementation, look in the AudioManager script and check the Main Camera component in the Main Menu scene.

# Multiplayer access is still not working properly, but players can currently drive around the track with implementation of the PlayerController and CameraFollow scripts.
